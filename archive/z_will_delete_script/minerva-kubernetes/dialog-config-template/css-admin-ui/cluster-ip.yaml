apiVersion: v1
kind: Service
metadata:
  name: cluster-ip-css-admin-ui
  namespace: dialog
spec:
  selector:
    app: deployment-css-admin-ui
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  