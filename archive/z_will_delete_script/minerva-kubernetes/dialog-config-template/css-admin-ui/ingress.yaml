apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-css-admin-ui
  namespace: dialog
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: css.local.mnv
    http:
      paths:
      - path: /
        backend:
          serviceName: cluster-ip-css-admin-ui
          servicePort: 8080
  tls:
  - hosts:
    - css.local.mnv
    secretName: local-mnv