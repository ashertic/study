apiVersion: apps/v1
kind: Deployment
metadata:
  name: dialog-engine-ms
  namespace: dialog
spec:
  selector:
    matchLabels:
      app: deployment-dialog-engine-ms
  replicas: 1
  template:
    metadata:
      labels:
        app: deployment-dialog-engine-ms
    spec:
      containers:
      - name: dialog-engine-ms
        image: IMAGE_BASEURL_PLACEHOLDER/dialog/ms_dialog_engine:IMAGE_VERSION_PLACEHOLDER
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: microservice
        volumeMounts:
        - mountPath: /root/logs
          name: logdata    
        resources: {}
      imagePullSecrets:
      - name: meinenghua
      nodeSelector: 
        node-tag: DIALOG_NODE_SELECTOR_PLACEHOLDER
      volumes:
      - name: logdata
        persistentVolumeClaim:
          claimName: dialog-logs 
