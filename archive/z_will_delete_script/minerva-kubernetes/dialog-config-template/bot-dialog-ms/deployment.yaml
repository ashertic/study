apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-dialog-ms
  namespace: dialog
spec:
  selector:
    matchLabels:
      app: deployment-bot-dialog-ms
  replicas: 1
  template:
    metadata:
      labels:
        app: deployment-bot-dialog-ms
    spec:
      containers:
      - name: bot-dialog-ms
        image: IMAGE_BASEURL_PLACEHOLDER/dialog/ms_bot_dialog:IMAGE_VERSION_PLACEHOLDER
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: microservice
        volumeMounts:
        - mountPath: /root/logs
          name: logdata    
        resources: {}
      imagePullSecrets:
      - name: meinenghua
      nodeSelector: 
        node-tag: DIALOG_NODE_SELECTOR_PLACEHOLDER
      volumes:
      - name: logdata
        persistentVolumeClaim:
          claimName: dialog-logs 
