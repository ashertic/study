apiVersion: v1
kind: Service
metadata:
  name: {{name}}
  namespace: {{namespace}}
spec:
  selector:
    app: {{selectorLabel}}
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP