apiVersion: v1
kind: Service
metadata:
  name: {{name}}
  namespace: {{namespace}}
spec:
  selector:
    app: {{selectorLabel}}
  ports:
    - protocol: TCP
      nodePort: {{nodeport}}
      port: 15672
      targetPort: 15672
  type: NodePort