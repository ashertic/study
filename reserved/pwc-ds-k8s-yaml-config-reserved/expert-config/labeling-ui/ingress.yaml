apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-labeling-ui
  namespace: expert
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: reader.local.mnv
    http:
      paths:
      - path: /
        backend:
          serviceName: cluster-ip-labeling-ui
          servicePort: 8082
  tls:
  - hosts:
    - reader.local.mnv
    secretName: local-mnv
