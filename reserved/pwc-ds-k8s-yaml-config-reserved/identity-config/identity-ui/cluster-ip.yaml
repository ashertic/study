apiVersion: v1
kind: Service
metadata:
  name: cluster-ip-identity-ui
  namespace: identity
spec:
  selector:
    app: deployment-identity-ui
  ports:
    - protocol: TCP
      port: 9100
      targetPort: 9100
  type: ClusterIP
  
