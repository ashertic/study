apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-homepage-ui
  namespace: identity
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: www.local.mnv
    http:
      paths:
      - path: /
        backend:
          serviceName: cluster-ip-homepage-ui
          servicePort: 8082
  tls:
  - hosts:
    - www.local.mnv
    secretName: local-mnv