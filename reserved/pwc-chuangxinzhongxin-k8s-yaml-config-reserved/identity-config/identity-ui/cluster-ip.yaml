apiVersion: v1
kind: Service
metadata:
  name: cluster-ip-identity-ui
  namespace: identity
spec:
  selector:
    app: deployment-identity-ui
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
  