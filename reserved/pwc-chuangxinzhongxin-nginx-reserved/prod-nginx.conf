user  nginx;
worker_processes  1;

error_log  /var/log/nginx/error.log debug;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  600;
    client_max_body_size       100m;
    client_body_buffer_size    1m;
    proxy_connect_timeout   1200;
    proxy_send_timeout      1200;
    proxy_read_timeout      1200;
    proxy_buffers           32 4k;

    #gzip  on;

    include /etc/nginx/conf.d/*.conf;

    # homepage ui
    server {
      listen                     50000 ssl;
      server_name                smartdocumentanalyser.asia.pwcinternal.com;
      ssl_certificate            /etc/nginx/cert/server.crt;
      ssl_certificate_key        /etc/nginx/cert/server.rsa;
      server_tokens              off;
      ssl_session_cache          shared:SSL:1m;
      ssl_session_timeout        5m;
      ssl_prefer_server_ciphers  on;
      # ssl_protocols TLSv1 TLSv1.1 TLSv1.2;

      location / {
        proxy_pass     http://10.157.112.207:31001;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $remote_addr;
      }
    }

    # identity ui
    server {
      listen                     50001 ssl;
      server_name                smartdocumentanalyser.asia.pwcinternal.com;
      ssl_certificate            /etc/nginx/cert/server.crt;
      ssl_certificate_key        /etc/nginx/cert/server.rsa;
      server_tokens              off;
      ssl_session_cache          shared:SSL:1m;
      ssl_session_timeout        5m;
      ssl_prefer_server_ciphers  on;

      location / {
        proxy_pass               http://10.157.112.207:31002;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $remote_addr;
      }
    }

    # expert ui
    server {
      listen                     50002 ssl;
      server_name                smartdocumentanalyser.asia.pwcinternal.com;

      ssl_certificate            /etc/nginx/cert/server.crt;
      ssl_certificate_key        /etc/nginx/cert/server.rsa;
      server_tokens              off;
      ssl_session_cache          shared:SSL:1m;
      ssl_session_timeout        5m;
      ssl_prefer_server_ciphers  on;

      location / {
        proxy_pass               http://10.157.112.207:32302;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $remote_addr;
      }
    }

    # reader ui
    server {
      listen                     50003 ssl;
      server_name                smartdocumentanalyser.asia.pwcinternal.com;

      ssl_certificate            /etc/nginx/cert/server.crt;
      ssl_certificate_key        /etc/nginx/cert/server.rsa;
      server_tokens              off;
      ssl_session_cache          shared:SSL:1m;
      ssl_session_timeout        5m;
      ssl_prefer_server_ciphers  on;

      location / {
        proxy_pass               http://10.157.112.207:32305;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $remote_addr;
      }
    }

    # identity api
    server {
      listen                     60000 ssl;
      server_name                smartdocumentanalyser.asia.pwcinternal.com;

      ssl_certificate            /etc/nginx/cert/server.crt;
      ssl_certificate_key        /etc/nginx/cert/server.rsa;
      server_tokens              off;
      ssl_session_cache          shared:SSL:1m;
      ssl_session_timeout        5m;
      ssl_prefer_server_ciphers  on;

      location / {
        proxy_pass               http://10.157.112.207:31004;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $remote_addr;
      }
    }

    # expert api
    server {
      listen                     60001 ssl;
      server_name                smartdocumentanalyser.asia.pwcinternal.com;

      ssl_certificate            /etc/nginx/cert/server.crt;
      ssl_certificate_key        /etc/nginx/cert/server.rsa;
      server_tokens              off;
      ssl_session_cache          shared:SSL:1m;
      ssl_session_timeout        5m;
      ssl_prefer_server_ciphers  on;

      location / {
        proxy_pass               http://10.157.112.207:32301;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $remote_addr;
      }
    }

    # information extraction api
    server {
      listen                     60002 ssl;
      server_name                smartdocumentanalyser.asia.pwcinternal.com;

      ssl_certificate            /etc/nginx/cert/server.crt;
      ssl_certificate_key        /etc/nginx/cert/server.rsa;
      server_tokens              off;
      ssl_session_cache          shared:SSL:1m;
      ssl_session_timeout        5m;
      ssl_prefer_server_ciphers  on;

      location / {
        proxy_pass               http://10.157.112.207:32306;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $remote_addr;
      }
    }
}
